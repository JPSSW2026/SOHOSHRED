/** PLACEHOLDER — replaced by the trick-system workstream. */
export class TrickSystem {
  constructor(ctx) {
    this.ctx = ctx;
    this.current = null;
    this.combo = { tricks: [], score: 0, active: false };
  }
  update() {}
  onLanded() {}
}
