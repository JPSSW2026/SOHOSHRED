/** PLACEHOLDER — replaced by the trail/decal workstream. */
import * as THREE from 'three';

export class TrailSystem {
  constructor(ctx) {
    this.ctx = ctx;
    this.texture = null;
  }
  build() {}
  update() {}
  getTrackTexture() { return this.texture; }
  clear() {}
}
