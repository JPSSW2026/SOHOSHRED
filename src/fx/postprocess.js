/** PLACEHOLDER — replaced by the post-processing workstream. */
export function createComposer(ctx) {
  ctx.composer = null; // direct render until the post chain lands
  return null;
}

export function updatePost() {}
