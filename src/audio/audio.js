/** PLACEHOLDER — replaced by the audio workstream. */
export class AudioSystem {
  constructor(ctx) { this.ctx = ctx; this.muted = false; }
  async build() {}
  update() {}
  setMuted(m) { this.muted = m; }
}
